name: kff-backend
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - fastapi==0.128.8
      - uvicorn[standard]==0.34.3
      - sqlalchemy==2.0.49
      - asyncpg==0.30.0
      - alembic==1.15.2
      - pydantic==2.11.4
      - pydantic-settings==2.9.1
      - fastapi-users[sqlalchemy]==14.0.2
      - email-validator==2.3.0
      - python-multipart==0.0.20
      - httpx==0.28.1
      # BLIP (torch/transformers/safetensors) omitted from default env — large wheels; see requirements.txt comment.
      - pillow>=10.0.0
      - anthropic>=0.49.0
