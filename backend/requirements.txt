fastapi==0.128.8
uvicorn[standard]==0.34.3
sqlalchemy==2.0.49
asyncpg==0.30.0
alembic==1.15.2
pydantic==2.11.4
pydantic-settings==2.9.1
fastapi-users[sqlalchemy]==14.0.2
email-validator==2.3.0
python-multipart==0.0.20
httpx==0.28.1
# Thumbnails / profile photos (raster validation). BLIP captions are lazy-loaded in code;
# PyTorch + transformers are omitted from the deploy image (~GB). To enable BLIP locally or in a custom image:
#   pip install 'torch>=2.2.0,<2.8.0' 'transformers>=4.40.0,<5.0.0' 'safetensors>=0.4.0'
pillow>=10.0.0
# AI-assisted EN→ES translation in the admin panel
anthropic>=0.49.0
